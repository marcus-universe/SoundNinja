.DS_Store
node_modules
/dist

# Windows device name (git add -A mmaps it → "mmap failed: Invalid argument")
NUL
nul
*.af~lock~

# Nuxt generated files
.nuxt
.output
.cache

# local env files
.env.local
.env.*.local

# Steinberg ASIO SDK (not redistributable). `bun run setup:asio`
/vendor/asiosdk/
# Junction to LLVM bin (libclang.dll). Created by setup:asio.
/vendor/libclang/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
node_modules
agents
.agents
.agent
agent

